<%= form_for(@reply_topic) do |f| %>
  <% if @reply_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@reply_topic.errors.count, "error") %> prohibited this reply_topic from being saved:</h2>

      <ul>
      <% @reply_topic.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
